{
  "name": "pribeh-emocii",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Veľký príbeh emócií — prototypy na Cloudflare Workers (statické assety + Basic Auth + /api/ai)",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "openai": "^4.104.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
